# Copy this file to .env in artifacts/sie-engine/ and fill in real values.

# Port the Vite dev server runs on.
PORT=5173

# Base path the app is served from (keep as "/" for local dev).
BASE_PATH=/

# URL of the api-server, used by the frontend to make API calls.
VITE_API_URL=http://localhost:4000
