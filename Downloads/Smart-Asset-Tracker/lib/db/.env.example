# Copy this file to .env in lib/db/ and fill in real values.
# Used by `pnpm --filter @workspace/db run push` (drizzle-kit) to create/update tables.
# Should be the same value as artifacts/api-server/.env's DATABASE_URL.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sie
